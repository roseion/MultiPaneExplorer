# 注意事项

- 每次改动完成后，都必须创建一个对应的 Git commit，以便后续追踪和回滚。
- 每次改动后，都必须编写或更新相关测试，并在交付给用户前，确保所有测试和验证全部通过。

## 用户反馈（已修复 · 2026-09-19 来自老高，经 workbuddy 转达）

**Bug：点击关闭文件树后，文件树没有真正消失，而是留下占位符**

- 复现：点击关闭文件树的按钮后，文件树区域仍在布局中占位（残留空白/占位区域），没有把空间归还给文件列表等其他窗格。
- 期望行为：关闭文件树后应**彻底从布局中移除**（如 Grid 列宽设为 0 或移除该列），空间完全让给其余窗格；重新打开时再恢复原宽度。
- 修复（2026-09-19）：关闭逻辑其实已设 Width=0 并 Collapsed 了树与分隔条，但列定义的 MinWidth=160 把 Width 钳制住，留下 160px 占位列；修复为隐藏时同步归零 MinWidth、恢复时还原（commit 11a0362）。
